%h3 Upload file .CSV
%ul
  %li File yang diupload harus berformat .csv. Jika Anda menggunakan Microsoft Excel, bisa Anda buat melalui File > Save as > CVS (Comma Delimited).
  %li Karena fitur ini cukup memakan resource server, diharapkan recruiter membatasi penggunaannya.
.span8
  .well
    =form_tag recruiter_upload_url, multipart: true do
      = file_field_tag :file, multiple: true
      = submit_tag "Upload", :class => 'btn btn-primary'
